# 下一阶段工作入口（更新于2026-09-18）

进度：27个R0历史回放、首版R1测试和最小物理闭环已完成；D/E/F真实OSM地图已落地。当前设计见 `research_hscc2027/design/EXPERIMENT_PROTOCOL_v0.2.md`。以下旧入口保留为工作顺序，未完成项应以最新实现/场地报告为准。

最新用户顺序：完整R1 → D双任务单bay → F多任务资源执行 → E在900s内的期限/负载标定 → A/B/C统一signature及新语义fixture → R6的240个正式单元冻结。当前R1显式枚举已触及预设100万状态上限，G1保持关闭，不启动后续验收或R6批跑。6个R0桥接仍是正式运行前的附加门。详见 `research_hscc2027/design/EXECUTION_PRIORITY_v0.1.md`；下列旧入口仅作历史背景。

1. 根据冻结的配置和 run 清单完成逻辑单元完整性审计；核对原始、修订和额外控制的对应关系。
2. 在新实验代码工作区复用旧修正语义，配置独立输出根，运行必要的 R0 控制；旧归档只作为输入。
3. 建立单世界所有者、后台提案器和不可变快照；先以确定性提案器验证“等待期间物理状态继续演化”。
4. 验证请求/任务版本、操作幂等、原子资源取得、有限等待与迟到响应处置，记录可重建的事件日志。
5. 加入最小唯一载荷、真实地面车辆与共同就绪交接，再比较 X_FIX、X_LST、X_TX、X_FULL 及快速 B2。
6. 冻结开发/正式划分、外生随机流、共同信息权限和运行签名后，才安排正式矩阵。

新代码工作目录：`research_hscc2027/`。论文 Git 仓库：`manuscript_hscc2027/`。两者与 `legacy/20260918/` 分开。

Overleaf 当前检出的是 ACM 示例文件 `sigplan.tex`，不是已经完成的新论文，也没有完成会议模板合规核查。写作阶段应创建正式主文件并检查会议当前要求。
