# HSCC+ICCPS 2027 研究方向转向计划书

## 从地空服务重构比较，转向时限感知的异步运行时监督

**项目：** `AnXu-ITS/global-llm-mobility-reconfiguration`  
**原稿：** *A Ground–Low-Altitude Mobility Manager for Service Reconfiguration under Disruptions*  
**建议新题目：** *Deadline-Aware Runtime Supervision for Ground–Air Service Reconfiguration*  
**编制日期：** 2026-09-18  
**文档版本：** 1.0  
**性质：** 研究与迁移计划；不是已经完成的代码修改、实验报告或接收预测。

> **总决策：原始实验不删除；旧研究版本整体冻结；基础设施和场景构件选择性复用；新论文的主要比较全部在统一的新执行与物理语义下重跑。既不把旧结果直接换标签，也不全量重跑原来的 10,960 个主实验。**
>
> 优先顺序是：**证据冻结与修正版基线验收 → 异步执行与协议性质 → 物理交接 → 强基线下的机制验证 → 有效规模与少量真实模型调用。**

---

## 阅读导航

- 第 1–3 节：结论、核查范围、仓库风险与旧实验处置。
- 第 4–6 节：新研究问题、方法边界、具体代码改造。
- 第 7–9 节：新实验组、样本与资源预算、评价和统计。
- 第 10–12 节：版本与数据治理、里程碑、论文改写。
- 第 13 节：可直接交给本地 agent 的执行任务书。
- 第 14 节：证据来源与定位。

---

# 1. 一页决策摘要

## 1.1 “删除、归档、重做”分别适用于什么

| 对象 | 建议处置 | 理由 |
|---|---|---|
| 原始运行日志、失败记录、LLM 请求响应、旧图表数据 | **只读保留，不删除、不覆盖** | 保留结论来源、修正轨迹和负结果；以后可以回溯为什么转向 |
| 旧稿及其权威修订版、源代码与统计流程 | **冻结成可恢复的 legacy 版本** | 新论文不应破坏旧论文的复现入口 |
| 地图、路网、设施几何、F1–F6 故障构件、B0/B1/B2、公共接口与检查器 | **选择性复用，并通过回归测试** | 重用已有工程资产，而不是重新开发一个模拟平台 |
| 旧日志已经完整记录的统计问题 | **允许重新统计，但输出到独立分析版本** | 不改变闭环轨迹的问题可以不重跑 |
| 涉及异步推理、物理交接、真实地面车辆、资源争用的新主要结论 | **相关方法与基线统一重跑** | 这些改变会影响后续状态、选择和任务完成，旧轨迹无法代替 |
| 原 E1–E3 的全部组合 | **不默认全量重跑** | 按新研究问题选机制场景；不是保留多少旧实验数量的问题 |
| 从新稿主文移走的内容 | **移至历史附录或版本档案，不等于删除证据** | 论文篇幅取舍与研究数据保存是两件事 |

最重要的比较规则：

> **允许复用旧场景，不代表允许把旧 B2 的结果，直接拿来与新物理模型下的新方法比较。进入同一主要比较的所有方法，必须使用相同的环境、资源、信息权限和计时语义。**

## 1.2 新稿应该回答的核心问题

原稿主要问：地空协调能否改善运输服务，不同监督策略有什么差异？

新稿建议问：

> **当慢决策器基于旧观测工作，而运输、故障与服务截止时间持续演化时，执行系统怎样避免把仍然存在的服务恢复机会消耗在等待、过时提案、资源冲突和未完成的交接上？**

LLM 保留为一种慢提案器，不再承担“必须胜过启发式”的核心主张。原稿 E3 已未显示 B4b 相对 B1/B2 的显著系统损失改善；这应保留为研究事实，而不是通过挑选新场景掩盖。[M：§6.4，pp.20–21]

## 1.3 投稿定位

建议以 **Systems and Applications** 为主。只有当对自主决策模块的运行时保障确实构成主要贡献时，再同时选择 **AI-enabled Autonomy**。官网允许实质相关的多 track 选择，且明确“仅仅使用 AI”不要求选择 AI track。[C1]

截至 2026-09-18，官网确认：摘要截止 **2026-10-29**，全文截止 **2026-11-05**，均为 AoE；Regular paper 正文最多 **10 页，不含参考文献**，ACM 双栏、light double-anonymous。附录和补充材料细则仍待公布。[C2][C3]

---

# 2. 核查依据与已经发现的迁移风险

## 2.1 本文档核查到了什么，尚未核查什么

**已核查：** 用户提供的 25 页主文、13 页补充材料；公开仓库 `main` 的目录、复现说明、关键 runner、适配器、队列修正、统计与运行入口；会议官方网站。

**尚未核查：** 用户本地全部 `runs/` 和 `outputs/` 是否齐全；本地未提交修改；完整原始数据逐文件校验；实际安装环境；当前本地代码与公开 `main` 是否相同；实际仿真或 API 调用结果。

本次是**公开源码静态核查与研究设计**，没有执行 SUMO–BlueSky、没有运行测试、没有调用模型 API、没有修改远程仓库。公开 `main` 是可变引用，本次未取得经验证的固定 commit SHA；阶段 G0 必须记录本地与远程实际 SHA，不能把本计划的访问日期当成代码版本。

下文区分三种状态：**已核查事实、建议方案、实施后待验证**。所有新模块、新实验编号、预算和门槛均为建议，不代表已经存在。

## 2.2 仓库不是已经核验完整的“原始数据库”

`.gitignore` 明确忽略 `runs/`、`outputs/`、`archive/`。这不能单独证明所有历史文件均未被跟踪，但足以说明：**不能因为 GitHub 仓库可访问，就认为完整原始实验已有公共备份。**[R02]

`manuscript/REPRODUCTION_README.md` 也把父项目的原始运行目录列为复现输入，并说明投稿 ZIP 不重复打包原始日志。[R03]

因此，第一步不是移动或清理目录，而是核实：哪些文件在本地，哪些在 Git 中，哪些仅在外部存储，哪些已丢失。一个 Git tag 只能固定受 Git 管理的文件，不能替代被忽略目录的独立备份。

## 2.3 八项关键代码核查结论

| 编号 | 已核查事实 | 对新项目的直接影响 |
|---|---|---|
| A1 | 论文的同一命令去重修正位于 `manuscript/source/revision_v3/queue_fix.py`；它是局部子类覆盖。默认 `tools/run_experiment4.py` 的 4B 仍映射到原 `Experiment4BRunner`。[R04][R05] | **默认 E4 入口不等于论文修正版入口。** 新基线首先要吸收并验证正确的去重语义，不能把已修复缺陷当新方法的主要对手 |
| A2 | E4 的 `_run_manager()` 直接调用阻塞式 `manager.decide()`；待执行命令失败后还可能同步再次调用管理器。LLM 客户端也是同步网络请求。[R06][R16] | 不只是加一个延迟参数；必须拆开提案请求、结果到达和执行，清除故障/拒绝路径中的阻塞重入 |
| A3 | `SumoAdapter.get_route_eta()` 使用路径长度与限速计算自由流估计；函数明确没有用动态 `getTraveltime` 计算该 ETA。[R07] | 不能把旧 ground timer 描述为逐任务实测拥堵旅行时间。新任务需实际车辆执行，ETA 仅是决策输入 |
| A4 | `Experiment3Runner` 与 E4 runner 是继承 E2 的不同分支；多任务/复合故障与 E4 时间实验并非同一个完整运行时。[R06][R08][R09] | 不能简单继承 E4 就声称同时拥有 E3 的多任务与多故障能力；新运行时宜组合这些机制 |
| A5 | `run_is_current()` 主要检查矩阵版本/哈希和实验 arm，没有把代码、执行器、环境语义版本纳入完整签名。[R05] | 改代码后沿用旧目录，可能被误判为“已完成，无需重跑”。新实验必须使用完整签名和新的 run identity |
| A6 | 修订数据经过独立后处理；`finalize_data.py` 在通用分析后插入修正的 B0 D60。`tools/paper_metrics.py` 对任务相关的有效干预另作过滤。[R10][R11][R12] | 不能直接收集各目录的原始 `metrics.json` 就认为复现了论文权威数字；必须记录指标推导版本 |
| A7 | 现有 registry 没有载荷保管、逐任务地面车辆和同步交接状态；BlueSky 指令在模拟步中处理，发出不等于物理生效。[R13][R14] | 新日志要分开提案、提交、发出、应用、交接与完成；不能把服务切换当货物已转移 |
| A8 | 核心飞机创建使用固定 `FLEET`；E4C 的附加记录是不可用干扰项。[R06][R15] | 扩展有效规模必须同时增加实际 BlueSky 飞机、有效任务和资源争用，不是修改一个记录数量 |

这些核查说明需要升级的位置，但**不等于已在现有代码中实测发现双重占用、载荷复制或所有异步竞争错误**。相关性质必须在新实现中通过测试与日志验证。

## 2.4 需要保留的三个细节

**第一，去重修正有明确边界。** 现有覆盖比较完整的 `normalized_action` 字典是否相同。新运行时可设计稳定的操作标识，避免仅 ETA 等描述字段变化就重置等待；但这是新语义，不能悄悄改写冻结版的旧行为。[R04]

**第二，旧统计有“原始—修订”两层。** B0 D60 的 20 次纠正运行替代旧版相同逻辑单元，不是给主要样本增加 20 个独立观测。当前环境复现 B1 的 342 s 与档案 341 s 的 1 s 差异已有说明，不能误归因为去重修复。[S：§S4.1，pp.8–9]

**第三，文档与脚本还有小型一致性问题。** 复现说明写可选回归增加 27 次运行，当前 `run_regression.py` 默认清单是 20 个纠正 seed 加 6 个控制，共 26 次。可能另有单独控制运行；G0 应用本地清单解释差异，而不是按说明文字虚构运行数量。公开仓库已经存在，但部分旧说明仍写没有公开 URL，也应在新版本修正。[R03][R17]

---

# 3. 旧实验逐项处置表

## 3.1 正式矩阵映射

下表数量来自原稿 Table 4，是**论文定义的逻辑实验数量**，不是本次对本地文件做出的完整性认证。[M：Table 4，p.14]

| 旧实验 | 原主实验数量 | 原来支持什么 | 数据怎么处理 | 在新研究中的位置 | 是否需要重跑 |
|---|---:|---|---|---|---|
| E1：地面扰动、选择性空运 | 2,880 | 是否启用空运、及时性与 incumbent-service 取舍 | 全部冻结；保留首决策、抢占案例和配对统计 | 动机、无空中故障对照；向 R2/R3 提供场景构件 | **仅重跑进入新主要比较的代表场景，并重跑这些场景下全部纳入方法** |
| E1 B4a 接口消融 | 额外 180 | 显式候选信息支持的影响 | 保留历史结果和原 prompt，不默认再调用 LLM | 历史附录；通常不再是新稿主要贡献 | **默认不重跑**；只有新稿重新主张新环境下的接口效应才补 |
| E2：16 个单故障/上下文组合 | 3,840 | 受影响链路恢复与及时完成的区别 | 保存全部故障配置、动作链和条件分母 | R1 协议故障测试、R2 时序、R3 转运 | **按机制选择后重跑**；不能把旧受影响次数当新轨迹的分母 |
| E3 v2：复合故障与多任务 | 2,880 | 多任务、资源不足与服务损失 | 冻结 v2 权威数据；更早版本单列 | R2 争用与 R4 有效规模的任务生成基础 | **选择性重新闭环运行**；不要照搬 L1–L4 排名作为新机制结论 |
| E4A：观察周期 | 400 | 任务/故障发现滞后与调用负担 | 旧曲线和同秒事件次序保留 | R2 的观察—推理—执行分解 | **核心重做**；旧实验并非真正推理并行下的物理闭环 |
| E4B：执行等待 | 560 | 等待、命令替换与截止时间 | 以修正版为权威，原错误单元仍保留溯源 | R0 回归锚点；R2 主要时间机制 | **核心重做**；基线必须含旧去重修正 |
| E4C：无效输入记录扩展 | 400；其中 B4b 100 | 固定四架核心飞机时的输入负担 | 冻结，标注 inert-input 实验 | 可选系统附录；不作为有效规模证据 | **默认不重跑**；R4 是另建的真实有效规模实验 |
| S4.1：纠正与控制回归 | 不作为新增主实验单元 | 去重修正、事件相位与环境差异 | 作为 R0 回归证据保留 | 基线正确性验收 | 在新的独立输出路径重做必要控制，不覆盖旧档案 |
| S4.2：离线附加转运时间 | 复用 E2 轨迹，不是新闭环队列 | 哪些旧结论对遗漏交接时间敏感 | 保留假设和原计算 | R3 动机与测试设计 | **要支持物理重构结论，必须闭环重做** |
| 路网边界与形态统计修正 | 后处理，不是服务运行 | 几何描述修订 | 保留修订几何、计算脚本和边界 | 新场景仍采用同地图时直接复用 | 只改描述无需重跑仿真；换网络/路线则另当别论 |

主实验合计 **10,960**，加 B4a 额外 **180**。不要把纠正运行、控制运行、重试或同一结果的不同分析版本相加，得到虚高的“新总样本”。

## 3.2 优先抽取哪些旧场景

场景选择应由机制决定，先冻结规则，再看正式实验效果。

| 场景构件 | 选择理由 | 新实验要改变或补上的东西 |
|---|---|---|
| E1 Site A：地面仍及时、空运更快但需抢占 | 检验等待或预留是否不必要地伤害原服务 | 加真实执行链，报告全部任务成本，而不只看紧急任务 |
| E2：主空运失效但仍有可用备机 | 检验旧响应、替代服务与交接准备的关系 | 时间异步化，载荷到位和资源独占 |
| E2：空运方案被移除但地面仍可行 | 检验最后可启动回退的时间窗口 | 地面车辆、装载、路由和真实完成事件 |
| E3 `E3_L3_COMP_F1` 等并发医疗任务构件 | 检验多个任务是否同时把同一资源当作备用 | 联合资源管理，而非逐任务独立减去 ETA |
| E3 `E3_L4_A/B` 的有限复合构件 | 检验连续失效后如何降级、报告无法恢复 | 不要求所有任务都能救回，但不允许无限等待或隐去失败 |
| E4B 故障发生在待执行命令附近 | 检验同秒事件排序和命令替换 | 预先确定相位扫描，不只展示故障正好卡住命令的一次轨迹 |

E3 的精确场景名和第二任务释放时刻以可执行 YAML 为准；当前 v2 配置的第二紧急任务在 300 s 释放。不要沿用旧注释中的另一时间，也不要将 L1–L4 描述为只改变故障强度。[R18]

已有三个站点只能说明不同运输条件下的表现；路网、设施、航程和故障位置共同变化，不能据此识别独立的“城市形态因果效应”。[M：§6.5，pp.20–21]

## 3.3 什么可以只重新统计，什么一定要重新运行

**只重新统计的条件：** 问题完全由已记录事件回答，且不改变执行选择或轨迹。例如：重画命令时间线；按既定 seed block 计算效应区间；纠正几何边界；从已有完整事件中统计提案替代。

**必须重跑的条件：** 改变时间推进方式、资源所有权、地面载运过程、交接状态、观察权限、任务释放/故障过程、策略、执行器或会影响后续决策的 ETA。

特别禁止两种伪闭环：

1. 在旧完成时间上加上模型耗时，就称为“异步推理闭环”。
2. 在旧完成时间上加 30 s 装卸，就称为“物理交接实验”。

两者只能标记为轨迹固定的离线敏感性分析，不能替代重新调度后的轨迹。

---

# 4. 研究主线、贡献与不能越过的边界

## 4.1 三个拟研究问题

**RQ1：执行一致性。** 在重复、乱序、延迟、超时和多任务争用下，系统能否保持任务版本、资源归属和载荷阶段的一致性，同时避免无限拒绝或等待？

**RQ2：机会保留。** 相对于合理调参的固定超时，以及同样使用完整物理信息的简单最晚启动规则，联合管理时间与资源是否减少可避免的迟到？其收益是否只出现在资源冲突、交接未就绪或故障相位敏感的条件下？

**RQ3：系统代价与范围。** 上述机制在实际异步调用、显式地面/交接模型和更大有效负载下，带来多少执行开销、资源闲置或原服务损失？

## 4.2 拟形成的三个贡献

| 拟贡献 | 最低需要的证据 | 不足以构成贡献的东西 |
|---|---|---|
| 具有物理前置条件的异步服务执行模型 | 请求、版本、执行、资源、载荷状态定义及可追溯实现 | 只画一个 LLM → Checker → Simulator 框图 |
| 联合时间预算与资源/交接约束的监督机制 | 明确算法、强简单基线、消融；对所声明性质给出论证 | 只加入 timeout，或重写原稿的剩余时限公式 |
| 机制级与闭环级验证体系 | R1 协议证据、R2 时间机制、R3 物理因果、R4 运行负担 | 只扩大样本量，或增加几座城市 |

原稿已经有预计时间裕量公式，B2 已经包含迟到惩罚；因此不能把“首次考虑 deadline”写成新颖性。[M：式(8)、式(9)，pp.7、10]

运行时切换和黑箱控制器检查已有 Black-Box Simplex 先例；采样时间充分性和有界可恢复性也已有专门研究。新稿应解释服务交接、异步执行承诺和共享资源使问题增加了什么，而不是把 RTA 本身当创新。[L1][L2]

## 4.3 新颖性核查的门槛

在大规模运行前，建立一个小型对照表：现有运行时保障、最晚启动/超时调度、分布式任务提交与资源预留研究，是否已经覆盖本研究的组合问题。

每篇关键文献记录：控制对象、时间模型、备用方案假设、资源共享、物理交接、可证明性质、实验平台、与本方案的实际差别。若差别仅剩“把已有规则接到运输模拟器”，应进一步收窄并提出可检验的新机制，不能靠换术语增强贡献。

本计划提出的是**候选研究主线**，不是已完成的“首次提出”查新证明。

## 4.4 允许与不允许的最终主张

可以争取：特定运行假设下的提交一致性；有条件的有限等待/进展；所测场景中减少执行层引起的额外损失；明确边界下的计算负担。

不得自动推导：航空碰撞安全、适航认证、任意故障下按时完成、任意规模可扩展、所有 LLM 均有效、随机测试零违例等于形式化证明。

尤其需要保留失败区：如果任务刚出现时已无可及时完成的方案，或后续故障消灭全部备选，执行层不应靠伪造运输或删除任务制造“成功”。

---

# 5. 拟新增的方法：时间预算与执行承诺一起管理

## 5.1 运行时的职责划分

建议保留现有候选生成、确定性策略和物理适配器，将运行时分成四部分：

```text
物理世界 / 故障源 / 任务源
          │ 受声明的信息与观察时序约束
          ▼
快速监督器 ── 生成不可变快照 ──► 慢提案器工作进程
    │                              │
    │ 持续更新时间、资源和交接      │ 只返回结构化提案
    │ ◄──────── 响应队列 ───────────┘
    ▼
提案接纳 / 版本与资源检查 / 有限期预留 / 下发 / 生效确认
    │
    ▼
SUMO + BlueSky + 任务/载荷注册表

本地应急响应不等待慢提案器，也不等待其请求超时。
```

只有仿真主进程允许修改注册表和操作 TraCI/BlueSky。推理工作进程只能读取快照并返回结果。各实验 run 保持现有的进程隔离方式，避免多个 run 共享模拟器全局状态。[R05][R14]

## 5.2 等待预算不等于简单统一超时

针对任务 m、时刻 t 和一条当前可执行的回退计划，定义：

\[
B_m(t)=d_m-t-\overline{T}_{\mathrm{fallback}}(m,t)-\overline{T}_{\mathrm{monitor+issue}}-\varepsilon.
\]

其中：

- `T_fallback` 是回退计划从实际启动到完成的剩余时间，包含必要的到位、交接、设施等待和运输。
- `T_monitor+issue` 覆盖监测周期、处理与下发到生效的预算，避免每次“刚好到最后一秒”才发现应启动。
- `epsilon` 是预先固定或在开发集上确定的额外余量。

交接地点的到达可能并行发生。原载具与接替载具的同时就位等待通常应按两者就绪时刻的最大值计算，再加交接时间，不能把所有并行段不加区分地相加。具体公式由定义好的阶段依赖图决定。

**关键限制：**

1. 若这些量只是点估计或经验分位数，则只能称为预测等待预算，不是确定性保证。
2. 预算建立在仍可使用的回退资源上。两个任务同时引用同一辆车，不等于两个任务都具有有效备用计划。
3. 资源预留不能防止任意未来物理故障；保证必须明确哪些环境变化被排除或有界。
4. 时间预算应该随状态重新计算，但不得因收到重复响应、重置时钟或无限延期预留，使等待变成无界。
5. 无可及时完成回退时，不应把负预算强行解释为“马上执行就保证成功”；应进入尽力服务或明确升级状态。

## 5.3 提案与执行对象建议字段

以下是**新接口建议**，不是现有仓库字段承诺。

```yaml
proposal:
  request_id: unique_request_id
  request_seq: 4
  observed_sim_time: 330.0
  observed_event_seq: 148
  mission_id: M_NEW_001
  mission_version: 3
  referenced_resources:
    - resource_id: M_UAV_02
      availability_version: 7
  action_type: DISPATCH
  payload_id: PAYLOAD_001
  payload_version: 2
  intended_route_id: route_hash
  latest_admissible_time: 351.0
  input_contract_hash: sha256_of_complete_input
```

执行层还要记录 `operation_id`、接纳版本、预留到期、下发时间、生效时间和最终处置原因。`request_id` 标识一次推理，`operation_id` 标识有副作用的操作；不能把请求重复和操作重复混为一谈。

新归一化不能把一份过时响应悄悄改造成针对新任务状态的另一条指令。允许补全的字段、允许刷新估计的字段、必须使提案失效的字段，应在接口中分开定义。

## 5.4 资源与载荷约束

建议先采用**单仿真所有者中的原子提交**：在一次接纳/提交中检查相关任务和资源版本、确认阶段前置条件、取得需要的资源；失败时释放临时状态并明确记录。无需第一版就引入完整分布式数据库事务。

预留必须有期限、有释放事件、有容量限制，并记录其造成的机会成本。新方案不能靠长期占住所有备用飞机来改善紧急任务，而不计算其他任务的损失。

载荷至少区分：

```text
AT_ORIGIN → LOADING → ON_CARRIER → WAITING_HANDOFF
          → TRANSFERRING → ON_REPLACEMENT → DELIVERED
```

还有明确的 `UNAVAILABLE/LOST` 等失败状态。交接成功必须由原载具/载荷到位、接替载具到位、设施可用和处理完成共同触发，而不是由监督器的意图触发。

**同一载荷转运**与**源头重新补发**分开建模。后者需要库存与重新装载；不能默认血液或器官在原载荷丢失后都能无限复制。第一版可只支持一种语义，另一种明确排除，避免为了覆盖动作字典而扩大范围。

## 5.5 需要证明或验证的性质

| 性质 | 建议定义 | 关键反例 |
|---|---|---|
| 任务版本一致性 | 已过期的响应不能覆盖当前已接纳/执行的任务版本 | 超时回退后旧空运响应返回 |
| 操作幂等 | 同一操作的重复提交不重复消耗资源、不重置原等待 | 相同命令每次轮询都延后执行 |
| 资源独占/容量 | 不相容任务不能同时取得同一独占资源或超容量设施 | 两任务分别认为同一备机空闲 |
| 载荷因果性 | 运输下一阶段不能早于载荷和相关资源就绪 | 备机在原载荷抵达前“运走”该载荷 |
| 有条件进展 | 在回退持续可用、处理和下发有界等假设下，系统有限时间内提交有效操作或明确报告失败 | 无限拒绝、无限重规划或预留反复续期 |

提交成功不是运输完成。即使证明进展性质，也必须另做物理闭环评价，才能判断任务最终是否及时。

---

# 6. 代码迁移方案：保留旧入口，新运行时单独建设

## 6.1 模块级工作清单

| 已有位置 | 复用部分 | 新工作 | 验收重点 |
|---|---|---|---|
| `orchestrator/experiment1_runner.py` | B0/B1/B2/B4b 工厂、初始化思路 | 抽出可配置有效机队；不改变 legacy 初始化 | 新机队数量与 BlueSky 实际飞机数相同 |
| `orchestrator/experiment2_runner.py`、`failures/` | F1–F6 注入与本地应急 | 故障只发事件，不同步等待推理 | 模型卡住时物理时间与应急仍前进 |
| `orchestrator/experiment3_runner.py` | 多任务释放和复合故障配置 | 抽出任务源、故障源，接入统一新循环 | 实际事件序列与冻结 YAML 相符 |
| `orchestrator/experiment4_runner.py` | 观察/队列的旧机制与回归锚点 | 新异步请求/响应、同秒事件顺序、有限等待 | 旧重复命令缺陷不能重新进入比较基线 |
| `manuscript/source/revision_v3/queue_fix.py` | 论文修正版去重语义 | 在新基线中显式实现并测试 | 修正版控制时序可追溯 |
| `orchestrator/candidate_info_e3.py` | 多任务候选接口 | 加逐任务地面 ETA、交接就绪、资源依赖 | 所有策略获得同样的派生事实 |
| `orchestrator/registry.py` | 任务和航空资源结构 | 新载荷、地面车辆、预留与阶段状态 | 唯一载荷、资源容量、完整状态转移日志 |
| `orchestrator/sumo_adapter.py` | 地图、路由、车辆添加/改路线基础接口 | 逐任务出车、到位与抵达检测、装载/交接 | 不能只生成 vehicle ID 而继续用 ETA timer 完成 |
| `orchestrator/bluesky_adapter.py` | 飞机创建、轨迹、指令适配 | 关联操作 ID 与实际应用/状态变化 | ISSUED 与 APPLIED 分开 |
| `managers/llm_client.py` | 请求编码和响应记录 | 工作进程、超时、迟到结果、单调时钟测量 | 推理过程不直接改世界状态 |
| `tools/run_experiment4.py` | 每 run 子进程模式 | 另建完整签名、追加式 attempt、失败登记 | 同矩阵不同代码不得直接复用旧结果 |
| `tools/paper_metrics.py` 与 `revision_v3` 分析 | 任务相关动作过滤、配对思路 | 新 schema、新指标、新结果目录 | 旧指标定义不得被新同名列悄悄替换 |
| `tests/test_experiment4_acceptance.py` | 原验收与控制用例 | 新异步、资源、交接、进展和故障交错测试 | 同时验证性质与测试能发现注入缺陷 |

源文件定位见 [R06]–[R21]。这张表是迁移设计，不表示这些修改已完成。

## 6.2 建议新增目录

以下路径均为**建议新增**，实施时可调整命名，但必须保持新旧输出隔离。

```text
provenance/hscc2027/                 # 必须纳入版本管理，不放在 ignored archive 下
  legacy_inventory.csv
  legacy_freeze_manifest.json
  correction_map.csv
  migration_decisions.md
  preregistered_comparisons.yaml
  environment.lock.json

orchestrator/runtime_v2/
  engine.py                         # 仿真唯一所有者，统一任务/故障/时间循环
  proposal_worker.py                # 不可变快照、异步提案与响应
  admission.py                      # 接纳、幂等、版本和资源原子检查
  deadline_budget.py                # 预测预算或条件上界，明确模式
  reservation.py                    # 有限期预留和释放
  custody.py                        # 载荷与交接阶段
  ground_service.py                 # 逐任务地面执行
  event_log.py                      # 统一事件与状态转移日志

config/hscc2027/                    # 新矩阵、开发/正式划分、预算
schemas/hscc2027/                   # 运行签名、事件、指标 schema
analysis/hscc2027/                  # 不调用旧稿重写脚本
manuscript_hscc2027/                # 不覆盖 manuscript/overleaf
runs/hscc2027/                      # 可继续 Git ignore，但须外部备份与索引
outputs/hscc2027/                   # 可再生结果，始终带分析版本
```

不要把多个 runner 简单复制后各自维护。优先复用任务源、故障源、候选器、提案器和执行器等组件，再由一个新循环组合。

## 6.3 两种时间实验必须分开命名

### 模式 A：`ASYNC_EMULATED`

提案在某快照上形成，按预先定义的延迟事件到达；世界在这段时间继续演化。可加速运行，不必真实睡眠等待。

它适合覆盖时延、相位和故障组合，**是异步机制的仿真实验，不是真实模型在线耗时的直接测量**。确定性提案器应从各方法自己的当时快照计算响应，而不是复用别的方法已经执行过的完整动作轨迹。

### 模式 B：`ASYNC_NATIVE`

真实模型请求后台执行，仿真按明确的墙钟映射推进。优先使用 1:1 实时节奏，记录模拟步落后墙钟的误差和服务端结果到达时刻。

若使用其他倍率，必须说明时延如何映射；不能让仿真任意快跑，再把某次模型 30 秒响应解释成真实运输系统 30 秒。

即使异步客户端取消等待，服务器请求仍可能完成或计费；迟到响应应记录并拒绝/重新审查，不能从账本中消失。模型名称只能写记录到的配置别名与可验证响应元数据，不把无法确认的后端版本写成确定事实。

## 6.4 共同信息权限

所有执行器基线可以使用同样的快速监测通道、同样的任务/资源状态、同样的物理估计。慢策略使用何种观测频率是另一个受控因素。

不得只允许新方法读取即时模拟器真值，而要求基线依赖旧快照。执行层当前检查与策略层旧观测可以不同，但这种权限划分必须对所有比较方法一致。

---

# 7. 新实验组 R0–R4：每组有问题、对照和验收

这些编号仅用于新研究，**不能覆盖或重新命名原 E1–E4 数据集**。

## 7.1 R0：档案核验与修正版基线桥接

**目的：** 确定在开发前到底继承了哪个版本，防止把数据缺失、旧队列缺陷或环境漂移误当新方法效应。

**工作：**

1. 盘点全部旧 run 的身份、路径、状态、配置、关键日志和所属分析版本；与论文矩阵核对，缺失保持缺失标记。
2. 冻结原始与修订结果，建立 B0 D60 的替代映射，不覆盖原 run。
3. 在新输出位置运行必要确定性回归；保存实际软件版本和日志。
4. 在新运行时的 legacy-compatible 模式下，做若干无异步、无新增物理阶段的确定性轨迹对照。这是工程桥接，不作为新方法贡献。

**明确锚点：** 修正版 B0 D60 F360 的执行时刻 360 s、完成时刻 542 s，持续时间 242 s；D0/D30 控制持续时间为 182/212 s；F359/F361 的 B0 D60 持续时间为 242 s。B2 的真实空运改地面仍可能为 302 s，不能要求它跟 B0 一样。[S：§S4.1]

**处理差异：** 同环境下确定性控制应复现；若环境改变产生差异，先定位到命令序列、模拟步或适配器，而不是调整预期值让测试通过。文档已披露的 B1 1 s 差异单列，不允许扩大为任意容差。

**通过条件：** 主要来源可定位；修正生效入口明确；选定控制轨迹差异已解释；新输出路径不会覆盖冻结数据。缺少完整旧原始库时，可以开展新实现，但必须明确旧论文无法在当前可用材料下完整复现，不能写“全库验收通过”。

## 7.2 R1：执行协议、故障交错与有条件进展

**目的：** 让运行时机制拥有独立于 LLM 表现的可检验证据。

先建立小规模状态模型，例如两项任务、两架飞机、一辆地面车、一个交接位，并显式限制任务数、消息数、可用时间点和搜索深度。可以先做状态转移枚举与性质测试；工具选型后再决定是否实现有界模型检查，不以工具名称替代证明。

| 测试族 | 必须出现的情况 | 应检查的结果 |
|---|---|---|
| 重复 | 同请求重复响应；不同请求提出同一操作；仅 ETA 字段改变 | 不重复副作用；定义好的同一操作不重置等待 |
| 乱序 | 旧请求晚于新请求返回；不同任务响应交错 | 过期版本不能覆盖新版本 |
| 超时 | 回退已提交后原提案到达；服务器没有响应 | 有明确丢弃/重新审查原因；不无限等待 |
| 资源竞争 | 两任务同看见一架备机；预留在提交前到期 | 不发生不相容同时占用；冲突有后续处置 |
| 交接 | 原载荷未到、备机先到、设施被占用 | 无“瞬间交接”；允许合理等待，不依赖伪完成 |
| 撤销 | 下发前撤销；物理生效后又收到取消 | 不把已发生的物理动作当成可无成本回滚 |
| 故障 | 故障与响应/执行同秒；模型阻塞期间 C2 丢失 | 排序明确；本地应急继续；失效资源排除 |
| 活性 | 连续重规划、反复续租、持续拒绝 | 在声明的持续可行条件下满足有限进展；否则明确终止原因 |

每个测试族设置受控缺陷版本，例如关闭版本校验或让重复提交重置 due time，验证测试确实能捕获反例。**这些故意破坏的版本只用于测试有效性，不能作为论文主要竞争基线。**

报告状态空间大小、枚举边界、反例、超时和未覆盖区域。不能把“有限模型检查通过”写成完整 SUMO–BlueSky 或实际航空系统的普遍保证。

**最低产出：** 性质定义表、模型假设、可复现反例、测试覆盖和一个与实现事件日志一致的检查器。仅报告测试数量不够。

## 7.3 R2：异步时间管理与机会保留——主实验

### 对照设计：时间管理 × 跨阶段资源管理

首先构建所有方法共用的正确物理执行平台：当前状态检查、原子单步更新、基本幂等、载荷阶段约束和本地应急均对所有方法有效。然后比较额外的跨阶段计划管理。

| 执行器变体 | 等待/回退规则 | 跨阶段回退资源计划 | 比较目的 |
|---|---|---|---|
| `X_FIX` | 开发集调参的固定 timeout，超时使用相同确定性回退策略 | 当前状态检查，不提前形成联合备用资源承诺 | 可靠的异步工程基线，不含旧重复命令 bug |
| `X_LST` | 使用同样物理估计的简单 latest-start/剩余时限规则 | 当前状态检查；不联合保留跨阶段备用资源 | **必须有的强简单基线**，检验是否只是 timeout 自适应 |
| `X_TX` | 同 `X_FIX` 的固定 timeout | 资源依赖、有限期预留和交接阶段计划 | 隔离计划/资源管理的作用 |
| `X_FULL` | 动态时间预算与同样回退策略 | 结合资源和交接有效性的联合管理 | 完整拟议机制 |

注意：`X_FIX` 和 `X_LST` 也必须拒绝物理上不成立的动作。`X_FULL` 的优势不能来自其他方法被允许重复使用载荷，或缺少本来就应该有的检查。

各变体共用回退策略；其版本和参数冻结。固定 timeout 可在开发集上选择合理值，必须记录选择范围，不能随意设一个容易失败的数值。

另设 **直接快速 B2**：在各自真实计算耗时下立即决策。它回答“为什么不直接用快策略”，不应被人为添加几十秒耗时后称为实际 B2。

### 提案器与实验角色

- **机制隔离：** 对同一种确定性/脚本化提案器施加匹配的外生延迟过程，比较四种执行器。每条响应基于该运行自己的请求快照；状态分歧后不强制复制另一方法的动作。
- **在线模型：** 在选定单元中使用真实 B4b 风格候选提案器，测量原生异步运行。模型不是唯一的有效性来源。
- **优化基线：** 条件允许时，增加小规模在线滚动时域优化，使用相同可见信息并计入求解耗时。知道未来全部故障的离线解只能作为参考上界。

### 场景与因素

建议先采用四类机制模板：无故障且选择有取舍；主空运失效而备机可行；空运失效而地面可行；双任务竞争同一回退资源。主要开发站点使用 A。

核心受控延迟采用 `0, 5, 15, 30, 60 s`。这些是诊断设置，不是声称实际部署时延服从该分布。120 s、长尾、丢失响应与乱序作为边界/压力扩展，不默认展开所有因素的笛卡尔积。

相位实验使用少量模板，预设故障相对于共同参考时刻的偏移，如 `−2, −1, 0, +1, +2 s`。所有比较方法必须接收同一外生事件过程。不要把故障分别设为“每个方法自己发出命令后一秒”，却称为相同环境；这属于另一个自适应压力测试。

### 主要输出

及时完成比例、事件到有效干预的时间、回退提交时剩余裕量、完整任务损失、资源闲置与预留成本、过时提案处置、额外重规划，以及监督层运行开销。

用时间线解释“等待 → 失去有效方案”的具体过程；总体统计中也应保留新方法无收益或更差的运行。

**重要门槛：** 若 `X_FULL` 相对 `X_LST` 的差别仅来自参数调节或不公平信息，而不是资源/交接机制，则研究贡献尚未成立。若快速 B2 已经足够好，应报告其适用区，不强行宣称慢模型有必要。

## 7.4 R3：显式载荷交接与逐任务地面运输——物理闭环

原补充材料显示 Site B 的一组及时替代服务只有 19 s 剩余裕量；离线加入 30 s 后，80 个服务会迟到。这里的价值是指出交接不能忽略，而不是证明新机制能挽救这 80 个服务。[S：Table S16、§S4.2，p.9]

**最小实现范围：** 一种医疗载荷、明确保管状态、原/接替运载工具就绪检查、一个可争用交接设施，以及逐任务地面车辆。先做好这一条链，无需立即做完整物流数字孪生。

**建议两个代表构件：** A 中地面与抢占空运均有可能及时的场景；B 中交接余量较紧的替代场景。保留其原几何和故障构件，但重新生成并验证物理可达、可交接的运行。

**处理时间设置：** `0, 15, 30, 60, 120 s`，全部标注为研究假设，不是现场测得的装卸参数。零处理时间仅用于分离固定处理开销，仍保留同地就绪、唯一载荷和实际运输约束。

四执行器使用同一物理规则。重点报告阶段等待、错过截止时间的原因、预留造成的原服务影响，以及估计完成与实际完成误差。关闭交接时间预测可作为估计消融，但不能关闭物理交接约束后把违例当“成功”。

加入有限的地面 ETA 误差或交通变化压力测试时，先区分自由流估计、在线预测与实际车辆到达。不要把真实地面车辆仅作为动画，任务却仍按 timer 完成。

**验收：** 每次完成能从日志重建唯一载荷路径；无无库存补发；交接发生不早于所有必需条件满足；处理期间的故障/新任务确实改变后续调度。关闭闭环更新应能改变至少一个事先设计的诊断案例，否则没有验证到该机制。

## 7.5 R4：有效规模与运行负担

建议从 **4、8、16 架实际有效飞机**开始，32 架留作增强项。必须同时检查：BlueSky 中实际创建数、候选中的有效资源数、服务兼容性、活跃任务数和设施容量。

三档负载不是只多加几条已完成任务记录。可用活跃任务数与有效医疗运输能力的比例定义，并在开发阶段确认确实产生低争用、接近容量和超载三种运行状态。

两种解释分开：

- 按相近负载比例扩大资源与任务，研究规模开销。
- 固定资源提高负载，研究竞争和降级。

建议先在 C 做新的规模迁移场景，或明确另选一处站点；它可以检验不同服务条件，但该城市已在旧研究中使用，不能称为“从未见过的盲测城市”。不同站点的规模结果也不能直接解释为城市形态因果效应。

至少比较 `X_LST` 与 `X_FULL`，报告候选生成、快速监督、提交、下发的耗时分布，真实延误、预留阻塞、队列长度和失败原因。统计实际测得最大值可以，不能未经论证就称作最坏执行时间保证。

**最低合格证据：** 更大输入确实对应更大的有效服务问题；计算代价和服务损失都可见。若增加资源使问题更容易而所有任务都轻松完成，这不充分说明负载能力。

---

# 8. 分阶段数量与资源预算

## 8.1 不立即启动最大矩阵

先用 3–5 个开发 seed 和少量模板验证机制。开发阶段可以改算法、参数和场景；正式阶段必须冻结这些选择，并使用不同 seed/实例划分。任何正式运行期间的方法改动都需要新的实验版本，不能只重跑表现差的单元。

下面是一份**可裁剪的初始规划**，不是已经完成的运行数，也不是经过功效分析后的固定样本承诺。

| 部分 | 初始配置 | 计划运行数 | 是否需要 LLM API |
|---|---|---:|---|
| R0 修正版控制 | 当前脚本默认 20 个纠正 seed + 6 个控制；另外核验历史额外控制 | 26 起 | 否 |
| R1 协议模型 | 有界枚举、性质测试、受控缺陷反例 | 单独统计状态/轨迹，不计作运输 run | 否 |
| R2 主要受控时延 | 4 模板 × 5 时延 × 4 执行器 × 20 seed | 1,600 | 否 |
| R2 相位扩展 | 2 模板 × 5 相位 × 2 强执行器 × 20 seed | 400 | 否 |
| R3 物理交接 | 2 构件 × 5 处理时间 × 4 执行器 × 20 seed | 800 | 否 |
| R4 小规模扩展 | 3 有效机队 × 3 负载 × 2 执行器 × 10 seed | 180 | 否；该样本先定位为规模探索 |
| 直接快速 B2 参考 | 4 模板 × 20 seed；按实际快速决策，不人为加入慢策略时延 | 80 | 否 |
| 原生异步模型验证 | 2 模板 × 3 执行器（FIX/LST/FULL）× 10 seed | 60 | 是，须先批准费用/调用上限 |

上述新确定性/受控仿真预算为 **3,060 次**；另有 60 次原生异步模型运行、R0 控制及开发测试。若 R4 增至 20 seed，再增加 180 次。任何额外站点、优化器或边界实验应单列预算，不隐含在这些数字中。

固定时延、固定场景中结果可能在所有 seed 上相同；重复数量不能创造实际不存在的随机信息。正式样本应根据开发阶段的变异、目标精度和资源情况确定，而不是反复追加直到显著。

## 8.2 真实模型调用要独立控预算

一个 run 可能有多次决策和重试，**60 个 run 不等于 60 次 API 调用**。

可讨论的首轮上限是：50 次测量/校准尝试，正式与重试合计最多 350 次，总计 400 次尝试。它只是建议上限，必须由用户确认；未确认时 agent 只运行不调用 API 的工作。

具体规则：在启动新的配对实验块前检查余量；到达上限停止并登记未完成单元；不能悄悄删掉昂贵或失败运行。若预算无法覆盖预定的完整比较，应先调整协议或申请预算，而不是选择性保留成功调用。

真实费用需要实际供应商计费规则、token 使用和调用次数，当前不虚构美元估计。请求失败时 token 未知应记为未知，不能统一写成零。

## 8.3 降低工作量的正确方法

优先用确定性提案器覆盖机制；只在关键单元调用模型。有限状态协议测试不需要完整物理仿真。地图与已有故障构件继续使用，先不下载更多城市。并行数量由单 run 的内存、端口隔离和模拟器行为实测决定，先低并发验收。

可以把历史耗时作为延迟模型的参考，但需要记录来源、输入规模和测量日期。历史动作只有完整输入、prompt、schema 和后端契约相匹配时才有缓存意义；新轨迹改变后的请求不能强行复用旧动作。

暂不投入：更大 LLM、教师蒸馏、实机飞行、50 架有效飞机、六故障全城市全矩阵交叉。它们不是当前主要证据链的先决条件。

---

# 9. 评价与统计协议

## 9.1 主要评价对象

建议以**所有已释放、在观察期内到期的紧急任务**为主要服务对象，并在配置中确保其 deadline 不晚于评价窗口。未在截止时刻完成的任务计为 missed deadline；不能只统计最终完成的任务。

可定义每运行结果：

\[
Y=\frac{\#\{\text{评价任务未按时完成}\}}{\#\{\text{评价任务}\}}.
\]

固定场景面板主要报告“各场景等权、各 seed 内配对”的 Y 差异。若另报把所有任务合并后的任务加权比例，要明确它改变了权重，不能与前者混称一个指标。

## 9.2 建议指标表

| 层级 | 指标 | 解释与注意事项 |
|---|---|---|
| 主要服务指标 | 紧急任务未按时完成率 | 包括未完成、失败和取消，不能只保留成功运输 |
| 机制 | 故障/任务事件到有效操作生效的时间 | 提案到达、ISSUED 和 APPLIED 分开；没有有效操作时不可伪记为零 |
| 机制 | 回退启动/提交时剩余裕量 | 标明预测裕量还是使用事后真实时间分析的诊断量 |
| 一致性 | 过时操作生效、重复副作用、资源超占、载荷违例 | 运行级和事件级均报告；零违例限于所测范围 |
| 进展 | 等待上界违例、无限重规划、无处置退出 | 与所声明的持续可行和有界处理假设对应 |
| 全系统 | 原服务中断暴露、延误、失败与预留阻塞 | 防止只优化紧急任务而隐藏其他任务损失 |
| 预测 | ETA 误差及预计及时但实际迟到次数 | 不把预测 margin 当作真实可恢复性 |
| 运行开销 | 监测、候选生成、提交等 p50/p95/p99 与实测最大值 | 控制层开销与 LLM 耗时分开；未证明的最大值不称 WCET |
| 模型接口 | 请求、重试、失败、超时、迟到响应、token 和费用 | 保留完整账本，非成功调用分母 |

“本来可以救回”的判断容易引入反事实偏差。除非有明确的同环境参考策略、可验证的可行性证据或小规模离线上界，否则只称“相对基线少迟到多少”，不要把所有失败都判为执行层本可避免。

## 9.3 旧指标不能直接改名复用

旧 SWL 使用平坦惩罚；旧 incumbent damage 统计终点状态，不是全程损失。旧 mission CSV 还遗漏部分抢占转移，因此不能把没有记录的中断时长当成零。[M：§5.5；S：§S3.2]

新版本应从每次状态改变记录开始计算时域内中断暴露。若只观察到 H，指标就明确是截至 H 的暴露，不声称已经知道完整最终中断成本。新损失定义使用新名称和版本，例如 `service_loss_v2`，与 `SWL_legacy` 分开。

完成时间对未完成任务是右删失或失败，不得赋为 H 后当真实完成时间平均。对于截止日在 H 之后的背景任务，报告观察边界，不因尚未到期便认定失败，也不认定其必然最终成功。

## 9.4 配对、随机性与检验

旧研究使用相同 seed 初始化多个固定场景，已有配对 seed-block 协议。重新分析旧数据必须保持这些依赖与原权重；不能把 10,960 个逻辑运行视为独立同分布样本。[M：§5.5；S：§S3.1]

新研究建议把外生随机流分为场景/交通、初始资源、故障、延迟和模型重复试验，并由稳定的种子派生规则生成。不能使用运行顺序或策略执行了多少动作来改变外生随机流；不同方法也不应该因为进程调度顺序不同而拿到不同故障序列。

主要比较预先指定为 `X_FULL − X_LST`；`X_FULL − X_FIX` 和 `X_FULL − X_TX` 作为同一主指标下的预设补充比较。记录效应量和置信区间，若进行显著性检验则对该预设家族处理多重性。次要指标与诊断另行标记，不随 p 值调整成“主要发现”。

原生模型调用还存在服务端负载与测量批次影响，不能只靠 temperature=0 就视为严格确定性。记录调用日期、批次和模型元数据，交错或随机化执行顺序；必要时按外生场景 seed 与批次分析依赖。

## 9.5 开发、正式与失败处理

开发集可用于调 timeout、余量与设施容量；正式集冻结后不可用其结果反向调参。旧论文已看过的城市和案例不叫全新盲测。

建立完整 run ledger：`planned / running / completed / failed / cancelled / censored / excluded_with_reason`。基础设施失败、策略无解、模型超时是不同原因；不能把它们统统丢弃后只算成功运行。

真正无效的基础设施运行可以重试，但每次 attempt 留档；所有方法采用同一排除协议。方法本身导致的失败必须留在服务结果中。

---

# 10. 数据、代码与结果版本治理

## 10.1 四层数据，不混池

| 数据层 | 内容 | 是否允许改变原始记录 | 是否进入新论文主要方法比较 |
|---|---|---|---|
| `legacy_original` | 原始 E1–E4、旧队列行为、原 API 日志 | 否 | 否；只用于溯源、诊断或明确标记的历史结果 |
| `legacy_revised` | 修正版分析、纠正 B0 D60、相应回归与图表 | 否；修正通过映射和派生结果表达 | 通常仅作为动机与工程回归，不与新物理结果混排 |
| `hscc_development` | 新模块测试、参数选择、诊断实验 | 允许产生新版本，不覆盖已记录 attempt | 不作为冻结后的确认性主要比较 |
| `hscc_primary` | 统一新环境语义下的全部纳入方法 | 原始 run 追加式保存 | 是；必须有完整签名、协议与运行状态 |

旧错误运行的合理处置是“保留且标记已被替代”，不是删除，也不是让它与修正运行一起进入均值。

## 10.2 建议冻结顺序

**步骤 1：只读盘点。** 记录本地仓库 SHA、工作区差异、远程配置和实际受跟踪文件。例如下列命令只读检查，不负责备份：

```bash
git status --short
git rev-parse HEAD
git remote -v
git ls-files runs outputs archive
```

若工作区有未提交修改，先保存补丁和文件哈希，不能为了获得“干净基线”而丢弃它们。

**步骤 2：建立外部备份。** 对原始 `runs/`、必要 `outputs/`、修订回归、论文源和依赖信息生成清单，保存到独立位置，抽样恢复并比对哈希。被忽略的 `archive/` 本身仍需备份，不是安全容器。

**步骤 3：固定代码与文稿。** 在核对实际提交与工作区后创建 legacy tag/分支，保存权威 PDF 和源文件的哈希。不要声称 tag 已包含未跟踪数据。

**步骤 4：在独立分支或 worktree 开发。** 旧文件可保持原路径，避免复现脚本的相对路径失效；新运行只写新根目录。若复制旧输入到新工作区，保持只读，防止脚本默认写回。

**步骤 5：记录纠正映射。** 至少保存原 run、替代 run、逻辑实验身份、纠正原因、执行器版本、统计入口和审核记录。

不要运行 `git clean`、`reset --hard`、强制推送或对历史目录使用 `--force`。不得把“重跑”实现成覆盖同名原始输出。现有 E4 入口能够重用原路径写日志，新入口应主动拒绝覆盖。[R05]

## 10.3 新 run 的完整签名

仅凭“相同 YAML 和 seed”不足以说明结果可复用。以下是**建议的新元数据结构**；null 表示实施前尚未填写，正式 run 不得保留必要字段为空。

```yaml
schema_version: hscc-run-v1
study_id: hscc2027_runtime_supervision
cohort: development
run_id: generated_from_full_signature
attempt_id: unique_append_only_attempt

source:
  git_commit: null
  dirty_patch_sha256: null
  dependency_lock_sha256: null

semantics:
  runtime_version: runtime_v2
  clock_mode: ASYNC_EMULATED
  sim_seconds_per_wall_second: null  # 加速离散事件模式无固定墙钟比例
  physics_version: ground_vehicle_custody_v1
  executor_variant: X_LST
  candidate_schema_version: candidate_v3
  metric_version: service_metrics_v2

scenario:
  matrix_sha256: null
  scenario_sha256: null
  network_sha256: null
  site: A
  environment_seed: null
  latency_seed: null
  backend_repeat_id: null

proposal:
  policy_id: B2_scripted
  prompt_sha256: null                # 不使用 prompt 时注明 not_applicable
  model_configured_alias: null
  model_reported_version: null       # 服务端未提供时保留 unknown，不编造
  request_contract_sha256: null

execution:
  status: planned
  start_utc: null
  end_utc: null
  failure_class: null
  parent_attempt_id: null
```

原生异步还需记录时钟倍率、节奏误差、机器负载和单调时钟耗时。每个输出文件与指标版本都应能追到同一 manifest。

缓存/跳过运行只允许在完整签名一致、必需日志齐全、终止状态合法且哈希通过时发生。代码或执行语义变化，不得因为旧 `matrix_hash` 相同就返回“已完成”。

## 10.4 原始记录最低要求

建议新 run 至少包含：

```text
run_manifest.json
run_config.yaml
events.jsonl                 # sim_time + event_seq + wall_monotonic
requests.jsonl               # 完整请求身份、输入哈希、状态、耗时
proposals.jsonl              # 原始响应、解析/归一化、拒绝原因
operations.jsonl             # 接纳、预留、下发、应用、取消/补偿
state_transitions.jsonl      # 所有任务、载荷、地面与航空资源转移
metrics.json                 # 可从以上记录重算，带 metric_version
checksums.sha256
```

原始 prompt/响应中可能存在 API 凭据、私有地址或识别信息。原始研究档案与公开复现包可以分开，公开前脱敏并记录转换；不能因为公开安全需要，就把研究证据本身悄悄删掉。

## 10.5 分析与论文构建隔离

保留旧分析链：

```text
inspect_evidence → morphology_boundary → verify_details
→ [optional regression] → analyze_revision → finalize_data
```

这条链用于旧稿复现。新的分析不应修改其输出；尤其不要在新论文目录调用旧 `rewrite_manuscript.py` 或 `refine_text.py`，它们以旧 baseline 为基础重生成内容。[R03][R12]

新的主表必须从 `hscc_primary` 清单生成；历史表来自 `legacy_revised`；图注和文件名直接写明语义版本。不要把两层数据放在同一图里而只用颜色区分方法。

---

# 11. 里程碑、继续条件与缩减规则

## 11.1 截止日期倒排

以下是以 2026-09-18 为起点的计划，不是对完成时间的保证。[C3]

| 时间 | 工作重点 | 必须交付的东西 | 不应做的事 |
|---|---|---|---|
| 09-18—09-24 | G0 档案、修正版入口、回归桥接；初步相关工作对照 | 盘点、冻结清单、修正映射、未解决差异报告 | 删除旧目录；启动全量 LLM 矩阵 |
| 09-25—10-01 | G1 异步循环与协议测试 | 模型等待期间物理演化证据；R1 性质/反例；完整事件时间线 | 只在旧同步循环里加 sleep 就叫异步 |
| 10-02—10-08 | G2 最小载荷交接与地面车辆 | 可追踪物理链、阶段断言、共同估计接口 | 用动画车辆和旧 timer 伪装实际执行 |
| 10-09—10-15 | G3 四变体开发实验、强基线、贡献检查 | 固定比较协议、参数选择记录、负结果、正式矩阵和预算 | 根据正式结果调方法或挑场景 |
| 10-16—10-22 | R2/R3 主要运行，R4 初步扩展 | 完整 run ledger、初版主图、失败原因分析 | 只保留成功运行；一边跑一边改指标 |
| 10-23—10-29 | 论文主稿、必要原生异步、摘要注册 | 主要证据已经可写；摘要在 10-29 前登记 | 把关键实验留到 rebuttal 再补 |
| 10-30—11-05 | G4 回归、匿名、复现与最终审查 | 冻结 PDF、结果索引、复现入口、最后校验 | 依赖截止后替换 PDF |

官网规定 author response 不能新增研究结果，全文截止也没有后续 PDF 更新窗口。因此核心机制证据必须在提交前完成。[C2]

## 11.2 五个验收门

### G0：来源可信

可用数据完成清单与备份验证；代码/文稿冻结；权威修正版入口明确；缺失和复现差异有记录。若原始库不齐，先保留可用材料并限制旧结果复现声明，不伪称完整。

### G1：异步不是标签

在一个可复现诊断中，请求未返回时飞机/地面/故障持续演化；迟到响应按版本规则处理；本地应急不等待模型；基本性质测试能发现人为注入的缺陷。

### G2：物理链成立

任务完成可以还原为唯一载荷和实际运载轨迹；无瞬移、无无库存补发；所有比较方法使用同一物理约束；交接准备与运输用时进入等待预算。

### G3：贡献超出强简单基线

`X_FULL` 的必要性不能只通过击败未修复旧队列或差参数 fixed timeout 证明。应看到跨阶段资源/交接处理带来的可解释贡献，或获得足够明确的性质论证和系统能力；同时保留 `X_LST`、直接 B2 同样有效甚至更优的区域。

若始终没有区别，不需要立即把整个方向判死刑，但应停止扩大重复运行。先判断是问题缺乏争用、方法无额外作用，还是实现不正确；分别处理。不能为制造优势而不合理地削弱基线。

### G4：可复现而且主张匹配证据

主要表可由冻结清单重算；没有新旧环境混池；失败与边界清楚；图文与真实方法一致；匿名检查和投稿状态无冲突。完成多少测试、使用哪个工具，都不能替代这些条件。

## 11.3 截止前工作量不足时怎样裁剪

**优先保留：** R0、R1、R2、R3，以及强简单基线。推荐的端到端物理服务主张依赖 R3，不能删除它却继续用相同标题和结论。

**先缩减：** R4 的最大规模、完整故障种类交叉、城市数量、真实 LLM 覆盖范围、额外优化器、图形装饰。

**可形成更窄版本：** 若物理交接来不及，只能把研究明确收缩为抽象服务执行协议，并在标题、模型和结论中撤回物理转运有效性的主张。是否有足够独立技术贡献需重新评估，不能把这个缩小版说成同样完成了原计划。

不建议为了赶期限先蒸馏或换模型。它们会引入另一条方法与数据验证链，但未解决当前最关键的执行和物理因果问题。

---

# 12. 论文重构与旧内容安置

## 12.1 主文建议结构

| 章节 | 新稿重点 | 旧稿复用方式 |
|---|---|---|
| Introduction | 异步监督可能浪费可恢复窗口；明确研究问题和贡献 | 用一个旧 E4 时间线/交接余量案例作有范围的动机 |
| Related Work | 运行时保障、时间与资源管理、地空任务执行 | 保留必要运输文献，减少大范围形态综述 |
| System Model | 时间、信息权限、任务/资源/载荷阶段、故障假设 | 候选接口与任务符号复用，明确新增语义 |
| Runtime Supervisor | 时间预算、接纳、资源计划、回退与性质 | 不把旧 Checker 改名就当新算法 |
| Implementation | 统一异步循环、两种时间模式、仿真接口 | 清楚说明旧平台继承关系与新增部分 |
| Evaluation | R1 协议，R2 机制，R3 物理链，R4 开销 | 旧 E1–E3 只在相关位置作历史或构件来源 |
| Limitations | 估计误差、故障假设、局部物理模型、规模、后端版本 | 保留原稿克制的证据边界 |
| Conclusion | 在什么条件下保留服务机会，什么时候必须回退或承认失败 | 不回到“协调总体有益”的泛结论 |

10 页正文应优先容纳方法、假设和主要结果。旧 25 页主文加 13 页补充材料不能简单整体压缩到会议模板；必须改变叙事分配。[M][S][C2]

## 12.2 建议主图

**图 1：** 两种决策速度、观察权限、请求/响应、提交、生效与物理交接的体系图。  
**图 2：** 一个完整反例及新机制处置时间线，标注所有关键事件与截止时间。  
**图 3：** 四执行器的机制消融，显示差异及不确定性，而不是只展示总均值。  
**图 4：** 交接和估计误差对及时性的影响，附阶段分解。  
**图 5：** 有效负载与监督开销/服务损失；篇幅不足则移补充，但主文保留关键数值。

旧 Supplement Figure S2 可作为历史诊断素材：B0 修正后保持原 due time，而 B4b 发生真正的空运改地面。必须说明那是旧服务语义下的记录，不是新运行时的结果。[S：Figure S2，p.12]

## 12.3 复用与匿名注意事项

旧稿若仍在其他期刊审稿，应在并行投稿前处理其状态；会议当前要求未发表且未在别处审稿的原创工作。[C2]

已有公开 GitHub 仓库不应为了匿名而删除。准备单独的匿名复现入口，按最终作者要求处理链接、作者名、文件路径和 PDF 元数据；不要谎称既有公开记录不存在。对于已经公开或发表的相近工作，如实引用并明确新增贡献。

官网尚未公布全部补充材料规则，因此不能把关键性质和主要对照全部放到“以后可能允许”的附录。提交前再次核对官方要求。[C2]

---

# 13. 本地 agent 执行任务书

以下内容用于用户将计划交给本地 agent 并授权实施后的工作。**本次文档编制没有执行这些步骤。**

## 13.1 任务目标

在现有 `global-llm-mobility-reconfiguration` 项目基础上建立新运行时，研究异步服务重构的时间预算、资源依赖与物理交接。保护旧论文和旧数据；不要全量重跑原矩阵，不要把目标改成“让 LLM 击败 B2”。

## 13.2 第一批必须交付的文件

建议在 `provenance/hscc2027/` 生成：

| 文件 | 必需内容 |
|---|---|
| `REPOSITORY_AUDIT.md` | 实际 SHA、工作区状态、本地与公开版差别、关键入口、依赖与阻塞调用链 |
| `legacy_inventory.csv` | 每个 run 的逻辑身份、物理路径、文件状态、数据版本、关键哈希；没有的写 missing |
| `legacy_freeze_manifest.json` | 原始数据、修订层、文稿与代码的冻结位置和校验信息 |
| `correction_map.csv` | B0 D60 原单元与纠正单元对应关系，以及其他已知修订 |
| `MIGRATION_DECISION_MATRIX.md` | E1–E4 每类数据保留、重分析、重跑的选择及理由 |
| `R0_REGRESSION_REPORT.md` | 实际运行控制、预期与观察、命令时序、环境漂移；不得只写 passed 数量 |
| `NEXT_STAGE_PLAN.md` | 第一个新异步诊断、所需模块和阻塞问题；未经预算确认不得安排 API |

只在实际生成并核对后声明上述文件完成。

## 13.3 执行次序

```text
A. 读取现有 README、原论文、修正层、runner 与统计入口
B. 盘点并保护旧数据，记录未提交代码，不修改旧原始结果
C. 在独立输出位置验证修正版基线与关键时序
D. 新建 runtime_v2，先让确定性提案器异步工作
E. 完成请求/版本/操作/资源状态日志与 R1 测试
F. 加最小载荷交接和逐任务地面车辆，统一所有比较方法的物理规则
G. 实现 X_FIX、X_LST、X_TX、X_FULL，先用开发集定位差异
H. 冻结正式协议、参数、随机流、比较与预算
I. 运行有限新矩阵，记录所有 attempt，失败不得静默删除
J. 只有获得明确调用预算后才开展原生模型实验
K. 从冻结清单生成新表图和论文；旧稿构建链保持独立
```

## 13.4 每轮实验后的报告格式

每个开发循环输出：本轮改变了什么；预先要检验什么；运行清单；结果与失败例；性质是否成立；与强简单基线的差异；下一步修改理由。区分“机制缺少收益”和“实现未通过基本正确性”。

正式运行循环不允许直接修改方法后继续向同一个 cohort 写入。发现实质问题时停止该版本，登记受影响范围，生成新版本与重跑清单。

## 13.5 禁止事项

- 不删除、移动到失联路径或覆盖 legacy 原始数据；不运行历史目录的破坏性清理。
- 不对原 E1–E4 使用 `--force` 批量覆盖；不强制推送；不擅自公开数据或凭据。
- 不把原版未修复 E4B 作为主要弱基线；不隐藏固定 timeout 或 `X_LST` 的合理调参。
- 不仅修改 `fleet_n` 就声称扩展有效机队；不只加 timer 就声称物理交接闭环。
- 不让推理 worker 修改模拟器；不在异步拒绝路径重新同步等待 `manager.decide()`。
- 不把旧 action cache 硬套到不匹配的新状态；不把失败调用和未完成任务过滤掉。
- 不把方案、测试预期或配置数量写成已观察实验结果。
- 未获批准不消耗模型 API 预算，不启动远程付费资源。

## 13.6 每阶段完成后必须回答的六个问题

1. 本轮是否修改或覆盖了任何 legacy 文件？应回答“没有”，并提供校验依据。
2. 新旧运行的环境、时钟、指标和执行器版本能否区分？
3. 比较方法是否享有相同的物理规则、信息权限与外生事件？
4. 失败、迟到响应、取消和不完整运行是否全部入账？
5. 新机制相对 `X_LST` 和快速 B2 的必要性是什么，是否有反例或负结果？
6. 当前证据支持哪一句论文主张，又不支持哪一句？

---

# 14. 来源与核查定位

## 14.1 用户提供的论文

**[M] 主文**：*A Ground–Low-Altitude Mobility Manager for Service Reconfiguration under Disruptions*，25 页，编译日期 2026-09-17。用户文件：`A_Ground_Low_Altitude_Mobility_Manager_for_Service_Reconfiguration_under_Disruptions (1).pdf`。本计划重点依据 §3–5 的系统与实验定义、Table 4、§6 的结果和 §7 的局限。

**[S] 补充材料**：同题 *Supplementary Information*，13 页。用户文件：`A_Ground_Low_Altitude_Mobility_Manager_for_Service_Reconfiguration_under_Disruptions (2).pdf`。重点依据 §S3 统计、§S4.1 去重修正、§S4.2 离线交接敏感性、Table S17 故障语义、Figure S2。

论文数字在本计划中作为作者现有报告使用，不表示本次已重新运行验证。

## 14.2 仓库来源

访问日期均为 2026-09-18。以下链接使用公开 `main`，可变化；执行 G0 后应补充真正的 commit permalink。文件路径用于精确定位，不代表本次下载了完整原始实验库。

| 标识 | 来源 | 重点定位 |
|---|---|---|
| [R01] | 仓库首页/README | 项目结构、复现范围与入口 |
| [R02] | `.gitignore` | `runs/`、`outputs/`、`archive/`、凭据忽略 |
| [R03] | `manuscript/REPRODUCTION_README.md` | 权威稿件、原输入路径、修正与统计顺序 |
| [R04] | `manuscript/source/revision_v3/queue_fix.py` | `DeduplicatedExperiment4BRunner._apply_valid_action` |
| [R05] | `tools/run_experiment4.py` | `RUNNER_BY_SUB`、`run_is_current`、输出路径与重跑行为 |
| [R06] | `orchestrator/experiment4_runner.py` | 阻塞决策、pending queue、E4A/B/C |
| [R07] | `orchestrator/sumo_adapter.py` | `get_route_eta`、`add_vehicle`、车辆/路线接口 |
| [R08] | `orchestrator/experiment3_runner.py` | E3 继承关系、多任务和复合故障 |
| [R09] | `orchestrator/experiment2_runner.py` | 故障注入与同步管理器调用 |
| [R10] | `manuscript/source/revision_v3/finalize_data.py` | 插入纠正 B0 D60、修订结果 |
| [R11] | `tools/paper_metrics.py` | 任务相关有效干预过滤与执行指标 |
| [R12] | `manuscript/reproduce_analysis.ps1` | 实际后处理执行顺序 |
| [R13] | `orchestrator/registry.py` | 当前任务/资源状态，无显式载荷交接 |
| [R14] | `orchestrator/bluesky_adapter.py` | 指令队列、模拟步、实际飞机状态 |
| [R15] | `orchestrator/experiment1_runner.py` | 管理器工厂、固定 FLEET、随机初始化 |
| [R16] | `managers/llm_client.py` | 同步调用、超时与后端元数据 |
| [R17] | `manuscript/source/revision_v3/run_regression.py` | 默认 20+6 回归清单、单独控制入口 |
| [R18] | `config/experiment3_matrix.yaml` | v2 场景、释放时刻、资源负载和 horizon |
| [R19] | `orchestrator/candidate_info_e3.py` | 多任务候选信息派生 |
| [R20] | `tests/test_experiment4_acceptance.py` | 现有 E4 离线验收范围 |
| [R21] | `manuscript/source/revision_v3/analyze_revision.py` | seed-block、配对、旧结果再分析 |

## 14.3 会议与相关工作

| 标识 | 来源 | 在本计划中的用途 |
|---|---|---|
| [C1] | HSCC+ICCPS 2027 Call for Papers | track 定位与贡献类型 |
| [C2] | HSCC+ICCPS 2027 Information for Authors | 篇幅、匿名、原创性、回应与材料要求 |
| [C3] | HSCC+ICCPS 2027 Important Dates | 摘要与全文截止日期 |
| [L1] | *The Black-Box Simplex Architecture for Runtime Assurance of Autonomous CPS* | 说明黑箱控制器与运行时保障不是空白起点 |
| [L2] | *Automating Recoverability Proofs for Cyber-Physical Systems with Runtime Assurance Architectures* | 说明采样时间充分性与有界可恢复性已有正式问题定义 |

[R01]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration
[R02]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/.gitignore
[R03]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/manuscript/REPRODUCTION_README.md
[R04]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/manuscript/source/revision_v3/queue_fix.py
[R05]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/tools/run_experiment4.py
[R06]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/orchestrator/experiment4_runner.py
[R07]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/orchestrator/sumo_adapter.py
[R08]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/orchestrator/experiment3_runner.py
[R09]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/orchestrator/experiment2_runner.py
[R10]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/manuscript/source/revision_v3/finalize_data.py
[R11]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/tools/paper_metrics.py
[R12]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/manuscript/reproduce_analysis.ps1
[R13]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/orchestrator/registry.py
[R14]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/orchestrator/bluesky_adapter.py
[R15]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/orchestrator/experiment1_runner.py
[R16]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/managers/llm_client.py
[R17]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/manuscript/source/revision_v3/run_regression.py
[R18]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/config/experiment3_matrix.yaml
[R19]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/orchestrator/candidate_info_e3.py
[R20]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/tests/test_experiment4_acceptance.py
[R21]: https://github.com/AnXu-ITS/global-llm-mobility-reconfiguration/blob/main/manuscript/source/revision_v3/analyze_revision.py
[C1]: https://hscc-iccps2027.taylortjohnson.com/call-for-papers/
[C2]: https://hscc-iccps2027.taylortjohnson.com/authors/
[C3]: https://hscc-iccps2027.taylortjohnson.com/dates/
[L1]: https://arxiv.org/abs/2102.12981
[L2]: https://nigam.info/docs/tase23.pdf

---

## 最终决策

**原实验作为历史证据与工程资产保存；新论文作为一个有独立问题、统一执行语义和新主要证据的研究版本建设。**

旧数据最重要的价值不是帮新稿省掉所有基线运行，而是已经指出了三个具体薄弱环节：**决策期间的时间是否真实流逝、指令是否仍能有效执行、服务替代是否具有物理上成立的交接链。**

把这三个环节做实，再用强简单基线检验新机制是否真正必要，比删除旧实验重新开始，或直接将旧实验换标签，都更合理。
