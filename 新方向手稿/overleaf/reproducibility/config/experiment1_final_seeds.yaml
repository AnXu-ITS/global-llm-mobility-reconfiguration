# Experiment 1 Finalization — frozen independent-seed list (§14).
#
# The primary statistical unit is scenario x simulation_seed (NOT same-prompt
# repetition).  These 20 seeds are FROZEN before the primary run and MUST NOT be
# pruned because a seed is unfavourable to any manager.  A seed may only be
# excluded for: simulator crash, corrupted artifact, or explicit protocol
# violation — each recorded in runs/experiment1_final/excluded_runs.csv.
#
# Seeds are 20 consecutive integers anchored on the canonical scenario seed
# 20240601.  Each seed drives BOTH the SUMO subprocess RNG and the deterministic
# per-seed initial-state realization (aircraft progress + battery/endurance
# jitter) in orchestrator/experiment1_runner.py, so different seeds genuinely
# change the manager-visible Global State (verified by SEED_INDEPENDENCE_AUDIT).

primary_seeds: [20240601, 20240602, 20240603, 20240604, 20240605,
                20240606, 20240607, 20240608, 20240609, 20240610,
                20240611, 20240612, 20240613, 20240614, 20240615,
                20240616, 20240617, 20240618, 20240619, 20240620]
