# Figure and manuscript verification

Final review covers the flexible-layout revision, not the archived boxed set. Figures 2–6 were inspected individually and on manuscript pages 12, 17, 18, 20 and 21. The main manuscript remains 25 pages; the supplement remains 13 pages. Both compile without overfull boxes or unresolved references. The only manuscript source change in this revision is the Figure 6 caption's description of nested markers.

All 36 bar means and original intervals, 48 heatmap cells, 12 supplementary paired contrasts and intervals, 240 Site A states, nine policy-choice fractions and 53 timing/token values are retained. Source-data contents are identical to commit `2f59b93ea81b158f80d4d0d30c2c60c05616e3c0`. No observations are excluded. The main text outside figures, tables, citation commands, bibliography and supplement source are unchanged. Figure 1's PDF/PPT and S1/S2 PDFs are byte-identical. S3's rendered image is identical; regenerated PDF/SVG metadata differ.

The main figures are fully vector PDF/SVG with embedded PDF fonts and editable SVG text. Figure 2/3/4/6 have a 9.5 pt source minimum; Figure 5 has a 10 pt minimum. The prior minima were 6.8/6.5/6.7/6.8/6.6 pt respectively. In the compiled main PDF the new minima are 7.572 pt (Figures 2/3/4/6) and 7.970 pt (Figure 5); most labels are larger. Panel titles are 12 pt in the exports. Supplementary S3 retains its previous type sizes. Exact dimensions, font measurements and asset hashes are recorded in `verification.json` and `manuscript_preservation.json`.

Plot alignment and rendered panel-letter baselines pass the 1.5 pt tolerance. Figure 6 intentionally uses two equally sized upper axes and a full-width lower axis; the builder also asserts that the lower axis shares their outside edges. Figure 4's scatter and choice matrix have aligned vertical extents. The Figure 5 colorbar remains vector and uses matching cell edges to avoid PDF-viewer seams.

Figures 3–6 and S3 have zero rendered collision failures or warnings. Figure 2's generic checker reports 18 text/stroke intersections and ten fill-edge warnings from roads/water beneath map labels. Its checker does not account for later opaque backing. A separate paint-order check verifies all 19 geographic labels have an opaque white backing after underlying paths, with no later visible stroke crossing the text. There are no text/text overlaps. All map labels were also inspected visually; an H1/V3 conflict introduced by the larger type was corrected.

The source validator's sans serif font whitelist flags Times New Roman. This is an intentional, documented exception to its generic default: the font matches the supplied Figure 1 reference and is installed and embedded. PNG files are 300 dpi previews; PDF/SVG are the authoritative vector exports. The numeric deadline legend and sparse white-backed callout were reviewed. No unresolved rendered findings remain.

The preceding boxed figures and corresponding documents are preserved at `archive/figures_boxed_20260914/before_flexible_layout.zip`. Earlier archives remain intact. Python generation and manuscript compilation were run; the retained PowerPoint export branch did not need to be rerun because Figure 1 and S1/S2 were unchanged.
