# R1 v0.3：有限边界内G1通过

2026-09-18。根据本轮自主执行指令，将有界安全验证从受状态数量限制的显式BFS扩展为Z3 SMT/BMC。**在声明有限模型中，深度1–12未发现六项安全性质的反例；G1在这一范围内通过。** 这不是整个真实系统的形式化证明，也不证明运输一定及时。

汇总证据：[G1 v0.3验收报告](../runs/protocol/r1_acceptance_v3_20260918_183450_65042f/report.json)。原 [v0.2记录](R1_ACCEPTANCE_v0.2.md)和100万状态BFS未完成结果保留。D/F/E、正式fixture及R6不因此自动通过。

## R1-A：有界安全

边界不变：2任务、2飞机、1地面车、1交接位；每任务最多2请求、全局最多4响应、每响应最多1副本；时刻0–4、最大12转移。初态、资源束、版本/租约、payload抽象继承v0.2。使用81个8位状态字段，12步内每个版本字段最多增加12次，不发生回绕。增加无状态变化的拒绝/noop步骤只允许补齐路径长度，不增加深度不超过k时可达的安全违例。

| 性质 | 深度1–12 | 使用证据 |
|---|---|---|
| 版本一致 | UNSAT | 每深度独立查询 |
| 幂等及固定due | UNSAT | 每深度独立查询 |
| 资源容量及原子取得 | UNSAT | 11个逻辑等价子性质，每个深度独立查询 |
| 载荷保管者唯一 | UNSAT | 每深度独立查询 |
| 载荷转移因果 | UNSAT | 每深度独立查询 |
| 取消历史一致 | UNSAT | 每深度独立查询 |

直接合并查询中，容量在深度9–12出现15秒超时；第一层拆分中，原子性在深度12出现20秒超时。随后只把违例析取式拆成更小查询，**未删除状态、限制动作、假设中间不变量或改变转移关系**。最终使用192个UNSAT查询合成六项结果。验收程序验证了各版本转移AST相同，并用SMT验证拆分前后的违例表达式等价。UNKNOWN保持原样，未改写成UNSAT。

直接查询：[首轮](../runs/protocol/r1_smt_20260918_182552_20e1c9/report.json)；[容量拆分](../runs/protocol/r1_smt_20260918_182936_141633/report.json)；[原子性细分](../runs/protocol/r1_smt_20260918_183226_68abb7/report.json)。各目录保存SMT-LIB查询、深度/状态/耗时、源码副本和hash。查询文本hash按UTF-8及LF规范化；gzip文件另有原始字节hash。

7个符号mutant均检出最短反例：跳过版本5步、重置due 4步、允许冲突5步、跳过交接条件1步、撤销已应用历史5步、非原子取得5步、终态继续hold 2步。最短性来自所有更浅深度UNSAT；见 [mutant证据](../runs/protocol/r1_smt_mutants_20260918_182743_a3ff32/report.json)。后两项是额外符号故障；前五项的SAT反例已在实际故障内核中重放，并由独立状态检查器检测。

求解器为固定 `z3-solver==4.15.4.0`，隔离安装在 `vendor/python_verification/`；API与安装依据为 [Z3官方仓库](https://github.com/Z3Prover/z3)和[官方Python API](https://z3prover.github.io/api/html/namespacez3py.html)。

## R1-B：有条件进展

新运行的80组时序组合全部通过：若同一备用方案持续可行，监测、计算和下发分别有界M、C、I，且重规划噪声不能后移保存的w，则 `commit_time <= w+M+C+I`。本组M=C=I=1 tick，从初始事件计还加有限等待w；不推导原生进程WCET或运输deadline保证。主协议中的2秒monitor/issue设计预算仍需实际适配器统一标定。

## R1-C：实现对应

490条路径、5,844次转移逐个比较：具体模型后继、实际内核执行后投影、SMT唯一后继。每次先确认该SMT转移SAT存在，再确认与内核后继不相等的查询UNSAT，避免因转移不可行而虚假通过。13种动作全部覆盖，含90个系统边界前缀；这是对应证据，不是穷尽的实现精化证明。见 [三方对应报告](../runs/protocol/r1_smt_conformance_20260918_1828/report.json)。

另重新运行48条原语义轨迹、10条回归、6种内核mutant、9条mailbox测试、80组条件进展；原A实际闭环的663个协议转移通过同一检查器。见 [组件及SAT反例重放](../runs/protocol/r1_components_v3_20260918_183148_2b194b/report.json)。实际到位、空中/地面动力、完整运输初态和新增跨阶段执行适配器由后续物理验收承担，不属于本有限模型的环境真实性证明。

## 执行状态

当前门配置为 `config/execution_gates.v2.json`。G1通过后自动进入D，再F、E和A/B/C统一；最后才允许正式冻结。任何影响已认证内核或模型的修改都须重验并更新hash；本轮尚未启动任何正式运输实验。
