# G-D：双任务单bay物理验收

2026-09-18。**G-D通过开发物理验收**，证据为 [四组配对报告](../runs/development/d_suite_20260918_184336_439352/report.json)。不是正式X_LST/X_FULL实验，也不表示已冻结D的R6 fixture。

D_03地图、网络与设施保持不变。两项独立载荷于300 s开始装载、315 s分别由两架BlueSky M600占位机实际起运，360 s触发共同返航；两辆实际SUMO车辆在V3的两个停车位待命，只有一个交接处理位。停车位采用SUMO off-road parking stop，均在建模设施范围内，不将两辆车的停车位置解释为两个bay。

四组是预留开/关 × 固定设施中断有/无，seed均为开发20270901。365 s提交地面接续；预留组使用当前物理距离/高度估计有限阶段窗口，末端为 `ceil(now+distance/15+altitude/1+handoff)+5+2`。该保守估计是开发参数，尚未作为正式共同估计器冻结。

无中断时两个载荷均在432 s满足共同就绪。任务1在432–462 s交接；任务2等待30 s，在462–492 s交接。实际卸载后送达为610/695 s。两种预留开关得到相同结果：**本构造中提前预留无额外净收益**。没有调整场景使其成为正收益案例。

中断组在共同绝对时刻440–450 s关闭设施；440 s先处理中断，交接保持原载具保管权，恢复后重新完成完整30 s处理。事件、轨迹和每个任务的重复开始时刻保存在运行报告，不以剩余ETA直接完成。

验收确认：实际双载荷/双车辆、真实单bay队列、最大并行交接数1、唯一保管权、SUMO实际到达、外生中断下保管权保留和重启、独立协议日志复核。配对组的物理输入及共同时间范围内空中轨迹完全相同，预留没有改变速度。地面到达仍受真实信号和排队影响。

`runtime_v2/stages.py`增加经记录的阶段资源释放和资源恢复；两者不取得资源或移动载荷。这些扩展API由本物理验收检查，不冒称已经包含在R1 v0.3的13类有限核心动作中。原核心文件未改动，G1证据保持有效。

后续自动进入G-F；D的正式reference/deadline及完整seed fixture仍需在共享冻结阶段完成。
